# MMLVE-Agent dependencies
#   pip install -r requirements.txt
#
# System requirement: ffmpeg / ffprobe must be on PATH.
#   macOS:  brew install ffmpeg
#   Ubuntu: sudo apt install ffmpeg

# HTTP client for the OpenAI-compatible and BytePlus ModelArk APIs
requests>=2.28

# Image loading, encoding, mask compositing
Pillow>=9.0
numpy>=1.24.0

# Video decoding and frame extraction
opencv-python-headless>=4.8.0

# Shot / scene cut detection
scenedetect>=0.6.4

# ffmpeg Python bindings
ffmpeg-python>=0.2.0
